Makefile:255: *** recipe commences before first target.  Stop.
int main ( int argc , char *argv[])
{


	int *p = malloc(sizeof(int));
	assert(p!=null);
	printf("(%d) address pointed to by p: %p\n", getpid(), p);
	*p =0;
	while(1) {
	Spin(1);
	*p = *p +1;
	printf("(%d) p: %d\n", getpid(), *p);
}
	return 0;
}
